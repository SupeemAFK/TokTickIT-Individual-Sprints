import { expect, test } from "playwright/test";
test("authenticated administrator can open user management", async ({page}) => { await page.addInitScript(() => sessionStorage.setItem("toktickit.token","admin-session")); await page.route("**/api/**", async route => { const url=new URL(route.request().url()); const body=url.pathname==="/api/auth/me"?{user:{id:3,name:"Aom",email:"admin@test",role:"ADMINISTRATOR",isActive:true,mustChangePassword:false}}:url.pathname==="/api/admin/users"?[]:{}; await route.fulfill({status:200,contentType:"application/json",body:JSON.stringify(body)}); }); await page.goto("/"); await page.getByRole("button",{name:"User Management"}).click(); await expect(page.getByRole("heading",{name:"User Management"})).toBeVisible(); await expect(page.getByPlaceholder("Search name or email")).toBeVisible(); await page.screenshot({path:"../artifacts/lab-03/screenshots/user-management.png",fullPage:true}); });

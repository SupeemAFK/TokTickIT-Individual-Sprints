import { expect, test } from "playwright/test";
test("authenticated administrator can search and create a user", async ({page}) => { let created=false; await page.addInitScript(() => sessionStorage.setItem("toktickit.token","admin-session")); await page.route("**/api/**", async route => { const request=route.request(); const url=new URL(request.url()); let body:unknown={}; let status=200; if(url.pathname==="/api/auth/me") body={user:{id:3,name:"Aom",email:"admin@test",role:"ADMINISTRATOR",isActive:true,mustChangePassword:false}}; else if(url.pathname==="/api/admin/users"&&request.method()==="GET") body=created?[{id:4,name:"New Staff",email:"new.staff@test",role:"IT_STAFF",isActive:true,mustChangePassword:true}]:[]; else if(url.pathname==="/api/admin/users"&&request.method()==="POST"){created=true;status=201;body={id:4,name:"New Staff",email:"new.staff@test",role:"IT_STAFF",isActive:true,mustChangePassword:true};} await route.fulfill({status,contentType:"application/json",body:JSON.stringify(body)}); }); await page.goto("/"); await page.getByRole("button",{name:"User Management"}).click(); await expect(page.getByRole("heading",{name:"User Management"})).toBeVisible(); await page.getByPlaceholder("Search name or email").fill("new"); await page.getByRole("textbox",{name:"Name"}).fill("New Staff"); await page.getByRole("textbox",{name:"Email"}).fill("new.staff@test"); await page.getByRole("textbox",{name:"Initial password"}).fill("InitialPassword123"); await page.getByRole("button",{name:"Add"}).click(); await expect(page.getByText(/User created/)).toBeVisible(); await expect(page.getByRole("cell",{name:"New Staff"})).toBeVisible(); await page.screenshot({path:"../artifacts/lab-03/screenshots/user-management.png",fullPage:true}); });

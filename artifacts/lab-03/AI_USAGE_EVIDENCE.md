# Lab 3 AI Usage Evidence

AI assistance was used as implementation support for the Lab 3 follow-up: reviewing the collaborator feedback, planning the authentication and role boundaries, drafting tests, checking the client workflows, and preparing the release evidence.

All final changes were inspected in the repository and verified with the commands recorded in `EVIDENCE.md` and `docs/lab-03/tests.md`:

- server API tests and TypeScript build;
- client component tests and Vite production build;
- authenticated Playwright requester and responsive checks.

The browser captures in `artifacts/lab-03/screenshots/` are deterministic API-fixture captures generated by the submitted Playwright test. They document implemented UI states and are not presented as live production records.

The implementation decisions remain reviewable in the source and test files, including:

- bearer-session identity and role checks in `server/src/lab3.ts` and `server/src/app.ts`;
- atomic requester ticket creation and staff claim handling;
- authenticated requester, staff, and administrator screens in `client/src/App.tsx`;
- API, component, and E2E coverage under `server/tests/`, `client/tests/`, and `client/e2e/`.
